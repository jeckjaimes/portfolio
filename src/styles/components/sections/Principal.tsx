import { makeStyles } from "@mui/styles";

// @ts-ignore
const principalStyles = makeStyles((theme) => ({
  root: {},
  container: {
    //height: "100vh",
    //background: "url(/principal.png) center/cover no-repeat",
  },
}));

export { principalStyles };
